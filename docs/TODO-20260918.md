# To-do — 2026-09-18

**Supersedes `docs/TODO-20260915.md`**, removed 2026-09-22 with the 09-13 and 09-14 lists; all three are in git history (`git show 994093f:docs/TODO-20260915.md`).

Ordering rule: **what expires first, then what is wrong right now, then what merely adds.**
🔌 needs the GPU · ⚡ needs elevation · 🕐 has a deadline outside this project's control

🛑 **Read counts off `python run_tests.py`, `python analysis/audit_claims.py --coverage` and
`python analysis/verify_citations.py --check`, never off this file.

🔌 **GPU work has ONE LIST PER CARD, since 2026-09-22:** [`5060 Ti`](GPU-WORKLIST-5060TI.md) ·
[`3070 Ti`](GPU-WORKLIST-3070TI.md) · [`2060 Super`](GPU-WORKLIST-2060S.md), with the shared protocol in
[`GPU-BENCH-RULES.md`](GPU-BENCH-RULES.md). Each carries its own **curves to build**.

🤝 **GPT has its own queue: [`docs/agents/GPT-QUEUE.md`](agents/GPT-QUEUE.md)** — its track record
across three prompts, and the next six jobs. Top of it is an adversarial audit of the **load-floor**
claim, because prompt C did exactly that to §5.7 and found the section could not support its own
sentence.**

---

## ✅ Closed since 2026-09-15 — the outside-reader exercise

**One paste into a fresh ChatGPT session, three PDFs read in full, and it was worth more than any
internal review this project has run.** Log: `docs/PRIOR-ART-20260918.md`.

| | |
|---|---|
| The cold novelty check | run at last. Seven retractions, and **no outside reader had ever been asked** |
| **Mendes, Tomás & Roma, JPDC 2022** | read in full. A **Vmin study on AMD** that **excludes NVIDIA in its own words** — a quotable justification, not a threat. ~~Closed the SBAC-PAD lead.~~ ⛔ **2026-09-22: it did not.** The SBAC-PAD 2020 paper was never opened; read that day, its Table IV re-locates the EDP optimum under a changed V–F relationship. `RELATED-WORK.md` §9. |
| **Guerreiro et al., HPCA 2018** | read in full. **Measured the constant-voltage region on a GTX Titan X and Titan Xp in 2018** with Afterburner/Inspector — four years before Schoonhoven, same tool lineage |
| **Guerreiro et al., TPDS 2019** | read in full. **The V/F response depends on HOW frequency is changed** — see item 4 |
| The droop hypothesis | GPT's best objection. **Tested and refuted** on two cards by a fixed-frequency test that needed no hardware |
| The negative control | **found partially leaky** — 4 of 12 workloads move. `CLAUDE.md` corrected |
| The 6 mV wording | **"one step of this sensor's resolution" corrected in three files including the paper**, 2026-09-18 |
| `verify_citations.py` | built; every arXiv id must be registered by someone who opened it. CI gate added |
| The 2104.00486 flag | **resolved, and the repository was right** — the preprint disagrees with the published TPDS version |
| `AGENTS.md` | was a 10,000-word duplicate of `CLAUDE.md`. Now a router, pinned by a similarity check |

## ✅ Closed later on 2026-09-18 — the bench session and prompts A + B

| | |
|---|---|
| **5060 Ti fine floor (4a)** | ✅ **RAN.** 0.720 V flat across **seven points, 1378–1560**, clean monotonic rise after. **No Turing-style non-monotonicity on this card.** |
| The floor END | ⚠️ **refined 1537 → between 1560 and 1590.** "1537" was never measured; it was inferred across a 157 MHz gap |
| Contamination vs voltage | 🔑 **measured**: throughput moved **+9.38%**, voltage moved **≤5 mV**. Voltage from contaminated sweeps is usable; throughput is not |
| Point-to-point diagnostics | ⛔ **shown unreliable** — blind to a uniform offset. Never certify a sweep clean from within-run shape |
| **Prompt B (XBAR)** | undervolt→XBAR is **January 2023** prior art; the three "independent" 2026 sources are **one author, one chip**; mitigations are published |
| **Prompt A (6 mV)** | 🔑 the grid is **per card** — 6.25 mV on three chips, **5 mV on the 5060 Ti**. "Sensor resolution" was wrong twice |
| Item 14 (what the 6 mV step is) | ✅ **answered** as far as public documentation allows |
| **Prompt C (adversarial audit)** | ⛔ **THE XBAR MEDIATION CLAIM IS RETRACTED.** Three claims verified against our own data — see item 8 |
| **Suite replicate r10** | ✅ first on driver 616.92, first with the operator deliberately absent. Peak `gemm` 16.819 vs r1–r9 mean 16.227 — in family, 2nd fastest |

---

# 🔌 GPU WORK — moved to one list per card, 2026-09-22

⛔ **Items 1, 2 and 4 used to live here**, beside the combined `GPU-WORKLIST.md`,
`5060TI-SWEEP-QUEUE.md` and `WEEKEND-PLAN-20260919.md`. **All three were removed 2026-09-22**; their open
items are in:

| card | list | curves to build |
|---|---|---|
| RTX 5060 Ti | [`GPU-WORKLIST-5060TI.md`](GPU-WORKLIST-5060TI.md) | ladder rung B, rung C, P4 at memory +0: one at a time into P1 |
| RTX 3070 Ti | [`GPU-WORKLIST-3070TI.md`](GPU-WORKLIST-3070TI.md) | stock, Edit 1, Edit 2, all before run 1. ⚠️ **Edit 1 would not apply — open** |
| RTX 2060 Super | [`GPU-WORKLIST-2060S.md`](GPU-WORKLIST-2060S.md) | stock, plus the Part 2 floor cap |

Shared protocol: [`GPU-BENCH-RULES.md`](GPU-BENCH-RULES.md). ⚠️ **The RTX 3060 has no list**: it is
not reachable. Its one open item was a fine floor plus a descending twin, ~25 min. Recoverable from
`git show 994093f:docs/GPU-WORKLIST.md`, item F, if it comes back.


## 3. 🕐 The Inspirit proposal — **get the dates, then submit**

`docs/INSPIRIT-PROPOSAL.md`. §6's deadlines are a placeholder; ~595 words against a 300–500 ideal.
✅ **Hand it to the mentor as the novelty check too** — GPT is not a substitute for a human reader
in the field, it is what was available first.


# 🆕 ADDED 2026-09-21 — from the bench-automation session


## 16. 🛑 The measurement guard does not cover a second agent, and four gates pass on bad data

**Demonstrated, not argued.** A Codex agent sharing this working tree was asked to change one
voltage reading in `rtx3070ti-20260825/hwinfo-silent/…_voltage.csv` (855 MHz, 0.819 → 0.900 V). It
did — and with the altered file in place **`audit_claims.py` reported 284 of 284, `run_tests.py`
828 checks all passed, and both `--check` gates exited 0.** Restored from git; nothing lost.

🔑 **`guard_measurement_files.py` is per-harness and therefore per-agent.** It is wired through
`.claude/settings.json`, so anything else with write access is outside it. **It stops one agent
making an accident; it is not an integrity control and must not be cited as one.**

✅ **The fix is DETECTION, not prevention: a content hash per measurement file, verified in CI.**
`data/MANIFEST.json` already censuses **367 files** with `path`, `category`, `rule` and `driver`,
and carries **no hash of any kind**. Agent-agnostic, survives a harness nobody has heard of yet.
⚠️ **Decide where the hashes live before writing any** — inside the manifest makes it self-
verifying but churns a generated file on every measurement; a sidecar keeps the manifest stable.
Record: `tools/claude-hooks/README.md`.


✅ **CLOSED 2026-09-22.** Built by GPT (Job 2), reviewed and widened to 1,106 files by Claude, and
running in CI. See `tools/claude-hooks/README.md`.

## ✅ 17–19 — closed 2026-09-22

| # | item | how it closed |
|---|---|---|
| 17 | Review Codex's time-join | reviewed and committed (`6382f1b`), then **verified on hardware**: 61 sweeps, 775 points, none empty |
| 18 | The Method 4 Reddit thread | GPT opened it: the stock-below-target shape is its **Method 3**. `CLAUDE.md` corrected |
| 19 | Prove the unattended chain | **61 of 61 sweeps** run with the operator absent, every log verified. The scheduled task was never registered; runs were driven from an elevated agent shell |


# 🔴 WRONG RIGHT NOW — free, no hardware

## 5. ⛔ THE CONTRIBUTION SENTENCE, THE CONTROL, AND A STALE CLAIM SOURCE

🆕 **Added 2026-09-19 from the load-floor adversarial audit, every figure recomputed here before
it was written down.** `CLAUDE.md` and `REGISTERED-PREDICTIONS.md` are corrected; **the paper is
not.** Four things to change:

**5a. The contribution sentence.** The paper's abstract-level version still says **"+465 MHz in 12
of 12"** and **"every prediction registered"**. Both are false as written. The median moved +465
with all 12 upward, **range +79.0 to +539.8**, 6 of 12 exactly +465 — and the decisive manipulation
was **post hoc**, in a run whose registered dose-response prediction failed. ✅ The detailed section
already has the per-workload wording right; it is the headline that does not.

**5b. "Region by region" overstates the manipulation.** P4 is +465 on P5 below 840 mV **and −98
above 875 mV**. Say so, and say why it does not carry the effect: a **570 MHz** edit in that upper
region (the control) moved the median by 0.

**5c. Causal n.** The curve was reshaped on **one** chip. The other three are stock-curve
associations and the fourth is undecidable. **Twelve workloads are repeated outcomes on one treated
unit**, which is exactly the framing item 6 already adopts — apply it here too.

**5d. §5.5.7's floor table renders 1552 MHz from a superseded extract.** The fine sweep bounds the
exit at **1560–1590**. `claims_consumer.py` names the old file, so the auditor is green. Point the
claim at `5060ti-finefloor-20260918/` and restate the row as a bracket.

🔑 **The audit also names the P4 identity problem:** the ABBA-era P4 plateau is **3015**, edited to
**3030** the same day. Any paper sentence that treats "Profile 4" as one configuration across
2026-09-08 needs a date beside it.

## 5e. 🆕 Restate the negative control in the paper — **it is partially leaky**

Same-session, n=12: median optimum **+0 MHz**, **8 of 12 unmoved**, **4 move** (bgemm32 +145,
layernorm +158, gemm +306, reduce +315). The treatment moved **all 12 by a uniform +465**.

⛔ **The paper and `CLAUDE.md` said "moved by nothing".** `CLAUDE.md` is corrected; **the paper is
not yet.** The contrast survives and is still large — but "clean null" is unavailable, and the
treatment claim is phrased per-workload, so the two must be stated on the same footing.

✅ **Do NOT concede the control itself.** The edited region *was* exercised — peak throughput rose
**+13% to +30%** at 2787–2978 MHz — so the null on the optimum is meaningful rather than vacuous.

## 6. 🆕 Adopt the outside reader's scope sentence verbatim

> *"In one physical GPU, a preregistered curve-region intervention shifted the measured efficiency
> optimum by 465 MHz for all 12 tested kernels, while a preregistered control-region edit produced
> no detectable shift. This **supports, but does not uniquely identify**, the constant-voltage
> boundary as the causal mechanism."*

🔑 Better than this project's own wording, and it comes from a reader with no stake. ⚠️ Amend the
control clause per item 5.

**Its framing of n is also better and should be used:** *experimental unit n = 1 chip; independent
interventions n = 1 treatment + 1 control; outcomes nested within.*

## 7. 🆕 Narrow "assumed rather than observed" wherever it appears

`CLAUDE.md` is corrected. **Check the paper.** Guerreiro et al. *measured* the region on consumer
NVIDIA in 2018 — the claim is true of **Turing** and of **Schoonhoven et al. specifically**, never
of consumer NVIDIA generally.

## 8. ⛔ §5.7 — THE OBJECTION LANDED. Rewrite the section for a retraction, not a pre-emption.

**This item used to say "§5.7 must pre-empt the bandwidth-saturation objection." It no longer
applies.** The adversarial audit made a stronger version of that objection and three of its claims
verify against this project's own committed data. `CLAUDE.md` is corrected; **the paper is not.**

⛔ **Retract from §5.7:**
- *"The two configurations agree exactly where their voltages agree… and diverge from the first
  point where stock raises voltage."* **False** — divergence starts two grid points earlier at
  **identical** reported voltages (−4.8% at 1477, −5.5% at 1560).
- *"DRAM sits at 16301 MHz throughout."* The only pair with voltage/XBAR telemetry logs **16301
  against 13801** — a 2500 MHz mismatch — and every run records a 7001 MHz minimum.
- **XBAR as the demonstrated mediator.** It may be a correlated indicator of a shared low-voltage
  policy.
- **The crossbar-to-core ratio as independent evidence.** It is arithmetic on two clocks already
  tabulated, not a second measurement.

⚠️ **And §5.7 must now confront its own §3.3.1**: the plateau begins at **280.6 GB/s** against a
documented *"ceiling near 281 GB/s that neither concurrency nor per-thread unrolling lifts"* — with
no curve manipulation. The section cannot claim the curve creates the plateau without addressing
that the card may simply be unable to escape a ceiling already present.

✅ **What survives and should be written plainly:** the plateau is real and reproduces, the repair
was derived from the diagnosis, stated in advance, and behaved as predicted, and the effect is
configuration-level on this unit. **Association plus a successful predicted intervention — never an
isolated causal chain.**

🔑 **The audit's own sentence is the one to use.** Full record and verification:
`docs/gpt-findings/2026-09-18-xbar-causal-claim-adversarial-audit.md`.

## 9. ⛔ THE INTRODUCTION STILL CARRIES THE RETRACTED §2.7 FRAMING

🆕 **Found 2026-09-19 while checking the consumer-artifact audit — it is not in that audit.**
`docs/PAPER_DRAFT.md` around **lines 140–147** states as live fact that the two consumer sets
*"both sweep core frequency only **at and above** the card's rated boost clock — 101–126% ...
95–118%"* and concludes they *"therefore cannot locate an optimum"*.

⛔ **That is the exact framing §2.7 retracted on 2026-09-13**, 330 lines later in the same document,
where the corrected table gives **89–111%** against the declared defaults and says in bold that the
sets are **not** overclocking sweeps. **The introduction and §2.7 now contradict each other.**

🔑 **Nothing could have caught this.** §2.1–2.5 are unaudited prose, the claims auditor only
checks strings it is told to check, and the retraction was written where the error was found rather
than everywhere it had propagated. **When a claim is retracted, grep the whole paper for its
numbers** — `101–126` and `95–118` would each have found it in one search.

⚠️ **And apply item 5's correction here too:** "cannot locate an optimum" is too categorical
outright (4 of 30 and 7 of 20 applications have interior optima). §2.7's own qualified form —
*"cannot locate an optimum **that sat at 62% of maximum clock on the V100**"* — is the one to use.

## 9b. Write the sweep-order caveat into the paper

Every floor extent came from an ascending sweep. The 3060 bound (flat across 10.2 °C) belongs
beside the floor table.

---

## 9c. ⛔ THE PREDICTOR'S 2.90x IS ONE CONFIGURATION — and the fine grid disagrees with the rule

🆕 **Added 2026-09-20 from the predictor audit; every figure recomputed here.** `CLAUDE.md`, the
tool itself and `analysis/models/README.md` are corrected; **the paper is not.**

- **Wherever the 2.90x appears, the decomposition must appear with it.** Drop `fulltune` and the
  mechanism, the single constant and the per-configuration constant are identical: **0.626% mean,
  77.8% exact, 144 curves.** The corpus is unbalanced 84/48/48/12.
- **Drop "extracts everything the configuration axis holds"** anywhere it survives. Both strategies
  emit **two** distinct frequencies; a tie between them bounds nothing.
- **Say that 720 mV is not identified by this data.** Every threshold from **711 to 741 mV** gives
  the identical table — a 31 mV window on a 5 mV card.
- 🛑 **And restate the rule itself.** On the one clean fine-grid record the efficiency argmax is
  **1402 MHz** while the floor runs to **1560–1590**. Efficiency is **flat across the floor and
  falls after it**, which is the ridge-point statement. The top of the floor is an **upper bound on
  where to run**, not a measured peak — and "the optimum IS the top of the floor" is a coarse-grid
  artifact.
- ✅ **Remove any suggestion that the V100 null and the consumer predictor disagree.** The V100
  model and the fixed baseline choose the same frequency on **32 of 33** workloads; the whole
  difference is `CNN_1.5M`. Neither analysis tests workload identity and finds it valuable.

🔑 **One item this creates for the bench:** the `reduce` residual is **16 of 16 missed, all
above** the prediction, with a fixed suite order confounding workload identity against position and
thermal history in 14 of 16 run legs. **A preregistered random-order replicated fine sweep of
`reduce` is the discriminator** — item 4m in `GPU-WORKLIST-5060TI.md`.

---

# 🟠 THE SPINE

## 10. Finish the dataset publication package

| piece | state |
|---|---|
| Licences | ✅ MIT + CC BY 4.0 |
| Manifest | ✅ reconciles at 360, pinned |
| **Top-level dataset README** | ⛔ missing |
| **Zenodo DOI** | ⛔ missing |
| **Licence scoping inside `data/`** | ⛔ `data/raw` and `data/external` are third-party |
| 🆕 **Hash provenance** | ⛔ our local third-party copies are **LF**; the audit reports the upstream files as **CRLF**, and converting ours reproduces its hashes exactly. **Record the retrieval path and line-ending convention beside any published hash** |

---

# 🟡 SCIENCE DEPTH

## 11. ✅ CLOSED 2026-09-22 — `verify_citations.py` checks DOIs

Built by GPT (Job 4) and reviewed: 13 DOIs registered, live-checked against Crossref (`ba54ca0`). The original item follows.

### ~~Extend `verify_citations.py` to DOIs~~

⛔ **It registers arXiv ids only.** All three sources the outside reader supplied are DOI-only and
had to be checked by hand. Crossref answers `https://doi.org/<doi>` with
`Accept: application/vnd.citationstyles.csl+json` — the same shape the arXiv path already uses, and
**the gap will only widen as the literature list grows.**

## 12. 🧪 The ML fourth predictor — needs no GPU

## 13. The remaining literature holes

⚠️ **Item 18 above is the one named, URL-in-hand lead**, and it is blocked on Raymond rather than
on effort. Everything below is search breadth; that one is a single page nobody has opened.

**~95 citing papers judged by title only** is still the largest. Plus the XtremeSystems thread and
bitcointalk 5249082. 🆕 **And the outside reader's own unreached list is worth working through** —
Scopus/WoS citation graphs from Schoonhoven, Guerreiro and Mendes; patent full-text on "piecewise
voltage frequency curve", "crossbar clock", "fabric clock"; Nouveau/envytools and VBIOS-table
work; forum search on **"SYS clock", "hub clock", "fabric", "L2 clock", "uncore"** rather than
"interconnect".

## 14. ✅ What the 6 mV step is — **answered 2026-09-18, as far as public documentation allows**

There is **no universal NVIDIA step**; NVIDIA's OpenVReg spec defines `Vstep = (Vmax − Vmin)/Nmax`.
Controllers differ (MP2884A command path 6.25 mV vs its own `READ_VOUT` at 1 mV; XDPE132G5C 5 or
10 mV; uP9512R 10 mV). **Measured here: 6.25 mV on the 2060 Super, 3060 and 3070 Ti; 5 mV on the
5060 Ti.** ⚠️ The grid is measured; **which controller is on these four boards is still unverified**,
and that is what remains open.

## 15. Stability testing tops out at thirty minutes

---

# 🟢 HYGIENE

## 16. Coverage — the unevenness, not the total
## 17. `distinct_clocks_measured` false-alarms on fine grids
## 18. Re-check the live community sources before submission
## 19. Parallelise the mutation runner — lowest priority, and it should stay there

---

## Not on this list, deliberately

⛔ **A version of the consumer-optimum claim this project owns.**
⛔ **A version of the narrow-window claim this project owns.**
⛔ **"We measured the constant-voltage region on consumer NVIDIA."** Guerreiro et al., 2018.
⛔ **"Nobody has modified a GPU V/F relationship and re-found its energy optimum."** Mendes et al.,
2022 — on AMD, where the APIs allow it.
⛔ **A second RTX 5060 Ti.**
