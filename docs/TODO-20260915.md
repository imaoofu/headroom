# To-do — 2026-09-15

**Supersedes `docs/TODO-20260914.md`**, which is kept for the reasoning behind items that closed.

Ordering rule: **what expires first, then what is wrong right now, then what merely adds.**
🔌 needs the GPU · ⚡ needs elevation · 🕐 has a deadline outside this project's control

🛑 **Read counts off `python run_tests.py` and `python analysis/audit_claims.py --coverage`, never
off this file.**

---

## ✅ Closed since 2026-09-14

| | |
|---|---|
| 2060 Super fine floor sweep | ran; `rtx2060s-finefloor-20260915/`. Did **not** settle the card — see below |
| The kit's missing guard | `Sync-Kit.ps1` shipped `Invoke-FrequencySweep.ps1` but never `WorkloadResultVerdict.ps1`, so the "did the benchmark run" check had its caller and not its helper |
| The join's bin width | ±25 MHz against a 15 MHz grid shared 908 of 916 samples; `--clock-tolerance` added, overlap now **measured** rather than predicted from geometry |
| Sweep order was not a variable | `-Descending` added. The tool sorted ascending with no switch, so every floor in the project was measured on a warming card |
| Paper sweep count | 359 → 360, 2060 Super 17 → 18, `--check` reconciles |

---

# 🕐 EXPIRING — these stop being possible

## 1. 🔌 RTX 3070 Ti Session D — the causal replication. **Still #1. Card leaves soon.**

`data/frequency-sweeps/rtx3070ti-20260825/SESSION-D-RUNSHEET.md`, predictions §4a–4b.

🔑 **Unchanged as the single item that changes what the paper can claim** — it converts one chip
into two chips and two architectures, which is the weakness a reviewer names in thirty seconds.
⛔ **Run the control or do not run the session.** Drop a replicate before dropping Edit 2.

✅ **Add one descending stock sweep while the card is there** — ~12 min, `-Descending`, stock. It
tests whether the §4d confound is card-specific or general, on a third architecture, for almost
nothing. Do it *after* the A/B/B/A so it cannot disturb the main result.

⚠️ **Afterburner is not in the kit and must be installed on that machine and removed afterwards.**
No script snapshots a profile store — copy the raw `Profiles\*.cfg` and record each SHA-256.

## 2. 🔌 2060 Super Part 1b — the descending sweep, **if the card is still reachable**

`SESSION-E-RUNSHEET.md` Part 1b, registered §4d. ~12 min, stock, nothing applied.

⚠️ **Lower priority than it looked yesterday.** The confound has been bounded from existing data:
the RTX 3060's floor is flat across a **10.2 °C** warm-up, moving less than one 6 mV code, while
the 2060 Super fell 13 mV across 13.5 °C. **The flat floors are probably genuinely flat**, so this
now tests a card-specific oddity rather than a project-wide invalidation. Still worth 12 minutes;
no longer an emergency.

## 3. 🕐 The Inspirit proposal — **get the dates, then submit**

`docs/INSPIRIT-PROPOSAL.md`, all seven sections drafted. Two things only Raymond can settle:
**§6's deadlines are a placeholder**, and it runs **~595 prose words against a stated 300–500
ideal**. ✅ Hand it to the mentor as the novelty check at the same time — item 6.

## 4. 🔌⚡ The two floor-ladder rungs on the 5060 Ti

Registered before collection, never built. Turns the causal result from *directional* into
*quantitative*. The 5060 Ti is the local machine, so this needs no card scheduling.

---

# 🔴 WRONG RIGHT NOW — free, no hardware

## 5. Narrow the causal claim's scope in the paper

**Still the highest-value thing that costs nothing.** The manipulation arm is **one chip**;
"+465 MHz in 12 of 12 workloads" is twelve *correlated* measurements on one GB206 under one curve
edit. ✅ The defensible sentence: ***strong within-chip causal evidence, generalisation uncertain.***
⚠️ **Do not concede the negative control** — sharing chip, session and operator is what a control
is *for*.

## 6. Ask the mentor: "is any of this new to you?" — **and now GPT too**

Seven novelty claims asserted and self-retracted, every one caught in-house. **No outside reader
has ever been asked.** One question.

✅ **A second channel opened 2026-09-17.** `docs/agents/NOVELTY-CHECK-BRIEF.md` is written to be
pasted cold into a fresh ChatGPT conversation — no project framing, because a model that has read
`CLAUDE.md` inherits this project's blind spots and stops being an outside reader. **Costs one
paste.** The mentor is still the better reader; GPT is the one available today.

## 6b. 🆕 Resolve the arXiv 2104.00486 author discrepancy

`verify_citations.py` flags it: this repository records the first author as **Wang**, arXiv orders
it **Mei** first. Same six people, different lead — and the short form "Wang et al." appears
throughout, on the source §2.7 now rests on. ⛔ **Open the published IEEE TPDS version.** Do not
edit either value to match the other; a preprint's author order is not authoritative for the
journal version, and this is the fifth citation question in a project that has already got four
wrong.

## 7. Re-weight the contribution sentence

Its opening clause describes standard undervolting practice. The load-bearing half is what follows:
the optimum tracks the **floor region** and not the region above it, with a negative control and
registered predictions.

## 8. §5.7 must pre-empt the bandwidth-saturation objection

The stock control and the crossbar-to-core ratio collapse already distinguish the plateau from
ordinary saturation; the section does not yet say so.

## 9. 🆕 Write the sweep-order caveat into the paper

Every floor extent in the paper came from an ascending sweep. The bound above (3060 flat across
10.2 °C) belongs in the text beside the floor table, with the direction stated — **not** left as a
thing a reviewer discovers. One paragraph, and it converts a latent objection into a stated limit.

---

# 🟠 THE SPINE — the contribution that cannot be pre-empted

## 10. Finish the dataset publication package

| piece | state |
|---|---|
| Licences | ✅ `LICENSE` (MIT) and `LICENSE-DATA` (CC BY 4.0) |
| Manifest + exclusion criteria | ✅ reconciles at 360 and is pinned |
| **Top-level dataset README** | ⛔ missing — scope, provenance, what a row means, how to cite |
| **Archive with a DOI** | ⛔ missing — Zenodo |
| **Licence scoping inside `data/`** | ⛔ `data/raw` and `data/external` are third-party and **not** redistributable |

🔑 Publishing the tree without scoping the licence would redistribute other people's data under
this project's terms.

---

# 🟡 SCIENCE DEPTH

## 11. Stability testing tops out at thirty minutes

Unchanged across three review cycles. Say "no failure observed in thirty minutes", never "stable" —
then extend one run.

## 12. 🧪 The ML fourth predictor — **needs no GPU**

360 sweeps are collected; arithmetic intensity, occupancy and memory traffic are computable from
them. 🛑 The deliverable is **when** per-workload prediction earns its cost, not a win.

## 13. 🆕 Re-examine the 5060 Ti floor at fine resolution

The 0.720 V floor rests on a **158 MHz** grid — three points. The 2060 Super looked flat at 60 MHz
and turned out non-monotonic at 15. ⚠️ Nothing says the 5060 Ti hides the same shape, and the flat
reading across 5.1 °C is mild evidence it does not, but **the most load-bearing number in the
project has never been measured at fine resolution.** Local machine, ~12 min.

## 14. The four literature follow-ups

`docs/PRIOR-ART-20260913.md`: the XtremeSystems thread behind the XBAR report, bitcointalk 5249082,
and **~95 citing papers judged by title only** — the largest hole in that pass.

✅ **The Mendes/Tomás/Roma lead is CLOSED, 2026-09-18.** The outside reader found a later,
openly-hosted paper by the same group (JPDC 165:32–51, 2022) and it has been **read in full** —
see `docs/RELATED-WORK.md` §8. It is a **Vmin** study on **AMD**, and it excludes NVIDIA in its own
words for lack of decoupled V/F control, which is a quotable justification for this project's
method rather than a threat to it.

🆕 **Two new leads in its place**, both unread: **Guerreiro et al. HPCA 2018**
(`10.1109/HPCA.2018.00072`) — reported to show the constant-voltage-then-rise structure on
Pascal/Maxwell/Kepler, four years before Schoonhoven and on consumer-class parts, which would make
it the **earliest direct measurement** the project knows of — and its TPDS 2019 follow-up
(`10.1109/TPDS.2019.2917181`). ⛔ **Guerreiro is now the higher priority of the two**, because if it
shows what GPT reports, it moves who owns the correlation.

## 15. 🔌⚡ The NVML clock-offset validation pair

Designed 2026-09-09, never run. **Claim nothing about the write until it has been exercised.**

---

# 🟢 HYGIENE

## 16. Coverage — the unevenness, not the total

**16 numbered sections carry no claims.** Carrying real numbers: **§5.4.2**, **§5.7**, **§5.7.7**.
Partially pinned: §5.8 at 6 of 31, §5.7.3, §5.7.4.

## 17. 🆕 `distinct_clocks_measured` false-alarms on fine grids

The sweep tool buckets clocks at 25 MHz, so the fine-floor run reported 10 against 13 genuinely
distinct achieved clocks, all locks held. Same stale assumption as the join bug, in a second place.
Low priority — it misreports, it does not corrupt.

## 18. Re-check the live community sources before submission

loong0x00, LACT #1147, mVolt+/kovasky. If any adds a bandwidth-plateau or crossbar-ratio result,
the crossbar claim narrows.

## 19. Parallelise the mutation runner

⚠️ **Lowest priority and it should stay there.** Nothing is blocked on it.

---

## Not on this list, deliberately

⛔ **A version of the consumer-optimum claim this project owns.** Four reformulations failed.
⛔ **A version of the narrow-window claim this project owns.** The dataset's own authors publish it.
⛔ **A second RTX 5060 Ti.** Effectively unobtainable since the price hike.
